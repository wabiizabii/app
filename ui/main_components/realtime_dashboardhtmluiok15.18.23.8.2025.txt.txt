<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Obsidian - Live Cockpit (Full Feature)</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <style>
        :root {--color-bg-primary: #0d1117;--color-bg-panel: rgba(31, 41, 55, 0.4);--color-border: #30363d;--color-text-primary: #c9d1d9;--color-text-secondary: #8b949e;--color-accent-primary: #1f6feb;--color-success: #238636;--color-warning: #e2b313;--color-danger: #da333d;--v-splitter-size: 16px;--h-splitter-size: 16px; --glow-color-default: rgba(31, 111, 235, 0.15);}
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { background-color: var(--color-bg-primary); color: var(--color-text-primary); font-family: 'Roboto', sans-serif; overflow: hidden; user-select: none; }
        .hidden { display: none !important; }
        h1, h2, h3, h4 { text-transform: uppercase; letter-spacing: 1.5px; font-weight: 500; }
        .ignition-module { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: 100; display: flex; flex-direction: column; justify-content: center; align-items: center; background-color: var(--color-bg-primary);}
        .ignition-module .header { color: var(--color-warning); border-bottom: 2px solid var(--color-warning); padding-bottom: 8px; margin-bottom: 24px; font-size: 1.5rem;}
        .ignition-module input { font-family: 'Roboto', sans-serif; font-weight: 700; font-size: 2.5rem; background-color: #010409; border: 1px solid var(--color-border); color: var(--color-text-primary); text-align: center; padding: 12px; border-radius: 6px; width: 350px; margin-bottom: 24px; }
        .ignition-module button { font-family: 'Roboto', sans-serif; font-weight: 500; font-size: 1.2rem; color: #fff; background-color: var(--color-accent-primary); border: none; padding: 14px 28px; border-radius: 6px; cursor: pointer; text-transform: uppercase; letter-spacing: 2px; }
        .ignition-module button:disabled { background-color: #2c333d; color: #5a6472; cursor: not-allowed; }
        #hud-bar { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
        .hud-card .value { font-size: 1.8rem; font-weight: 700; }
        .hud-card .sub-value { font-size: 0.8rem; color: var(--color-text-secondary); margin-top: 2px; }
        .portfolio-info-bar { margin-top: auto; padding-top: 8px; border-top: 1px solid var(--color-border); display: flex; justify-content: space-between; align-items: center; font-size: 0.8rem; color: var(--color-text-secondary); }
        .portfolio-info-bar strong { color: var(--color-text-primary); font-weight: 500; }
        .status-indicator-hud { font-size: 0.8rem; }
        .risk-dial { background-color: #010409; border: 1px solid var(--color-border); color: var(--color-text-primary); border-radius: 4px; padding: 2px 6px; font-size: 0.8rem; appearance: none; background-image: url('data:image/svg+xml;charset=US-ASCII,%3Csvg%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%20width%3D%22292.4%22%20height%3D%22292.4%22%3E%3Cpath%20fill%3D%22%23c9d1d9%22%20d%3D%22M287%2069.4a17.6%2017.6%200%200%200-13-5.4H18.4c-5%200-9.3%201.8-12.9%205.4A17.6%2017.6%200%200%200%200%2082.2c0%205%201.8%209.3%205.4%2012.9l128%20127.9c3.6%203.6%207.8%205.4%2012.8%205.4s9.2-1.8%2012.8-5.4L287%2095c3.5-3.5%205.4-7.8%205.4-12.8%200-5-1.9-9.2-5.5-12.8z%22%2F%3E%3C%2Fsvg%3E'); background-repeat: no-repeat; background-position: right 6px center; background-size: .6em; padding-right: 20px; }
        .progress-bar-container { width: 100%; height: 6px; background-color: #010409; border-radius: 3px; overflow: hidden; border: 1px solid var(--color-border); }
        .progress-bar { height: 100%; transition: width 0.3s ease; }
        .progress-bar.bg-blue { background-color: var(--color-accent-primary); }
        .progress-bar.bg-gradient-risk { background: linear-gradient(90deg, var(--color-success) 0%, var(--color-danger) 100%); }
        .progress-footer { display: flex; justify-content: space-between; font-size: 0.75rem; color: var(--color-text-secondary); margin-top: 4px; }
        .text-profit { color: var(--color-success); }
        .text-loss { color: var(--color-danger); }
        .text-warning { color: var(--color-warning); }
        #chart-area { flex-grow: 1; display: flex; align-items: center; justify-content: center; font-size: 2rem; color: var(--color-text-secondary); padding: 0; }
        #tradingview-widget-container {width: 100%; height: 100%;}
        .order-type-tabs { display: flex; gap: 8px; margin-bottom: 16px; }
        .tab-btn { flex-grow: 1; padding: 8px; font-size: 0.9rem; background-color: #010409; border: 1px solid var(--color-border); color: var(--color-text-secondary); border-radius: 4px; cursor: pointer; }
        .tab-btn.active { background-color: var(--color-accent-primary); color: #fff; }
        .planner-group { display: flex; flex-direction: column; gap: 4px; }
        .planner-group label { font-size: 0.75rem; color: var(--color-text-secondary); text-transform: uppercase; }
        .planner-input, .planner-select { background-color: #010409; border: 1px solid var(--color-border); color: var(--color-text-primary); border-radius: 4px; padding: 8px; font-size: 1rem; width: 100%; }
        .tpsl-group { display: none; }
        .lot-input-wrapper { display: flex; align-items: center; }
        .lot-input-wrapper input { flex-grow: 1; }
        .lot-input-wrapper .lot-adjust-controls { flex-shrink: 0; margin-left: 8px; display: flex; gap: 8px; }
        .btn-lot-adjust { background-color: var(--color-border); color: var(--color-text-primary); border: none; border-radius: 4px; width: 24px; height: 24px; cursor: pointer; }
        .execution-controls { display: flex; flex-direction: column; gap: 10px; }
        .btn-planner { border: none; padding: 10px 16px; font-size: 0.9rem; font-weight: 700; text-transform: uppercase; border-radius: 4px; cursor: pointer; }
        .btn-planner:disabled { background-color: #2c333d; color: #5a6472; cursor: not-allowed; }
        .planner-readouts { margin-top: auto; padding-top: 16px; border-top: 1px solid var(--color-border); display: flex; flex-direction: column; gap: 8px; }
        .readout-item { display: flex; justify-content: space-between; font-size: 0.9rem; }
        .readout-item .loss-percent { font-size: 0.8rem; color: var(--color-text-secondary); margin-left: 8px; }
        .table-container { overflow-x: auto; padding-bottom: 8px; }
        .h-splitter { cursor: ns-resize; height: var(--h-splitter-size); margin: calc(-1 * var(--h-splitter-size) / 2) 0; z-index: 10; background-color: transparent; }
        .select2-container--obsidian .select2-selection--single { background-color: #010409; border: 1px solid var(--color-border); border-radius: 4px; height: 38px; }
        .select2-container--obsidian .select2-selection--single .select2-selection__rendered { color: var(--color-text-primary); line-height: 36px; padding-left: 12px; }
        .select2-container--obsidian .select2-selection--single .select2-selection__arrow { height: 36px; right: 6px; }
        .select2-container--obsidian.select2-container--open .select2-selection--single .select2-selection__arrow b { border-color: transparent transparent var(--color-text-primary) transparent; }
        .select2-container--obsidian .select2-dropdown { background-color: #010409; border: 1px solid var(--color-border); border-radius: 4px; }
        .select2-container--obsidian .select2-search--dropdown .select2-search__field { background-color: #0d1117; border: 1px solid var(--color-border); color: var(--color-text-primary); border-radius: 4px; }
        .select2-container--obsidian .select2-results__option--highlighted[aria-selected] { background-color: var(--color-accent-primary); color: white; }
        .select2-container--obsidian .select2-results__option[aria-selected=true] { background-color: #2c333d; }
        @keyframes order-sent-flash { 0%, 100% { box-shadow: 0 0 12px 2px var(--glow-color-default); } 50% { box-shadow: 0 0 25px 8px var(--color-accent-primary); } }
        .order-sent-flash { animation: order-sent-flash 1s ease-in-out; }
        @keyframes standby-pulse { 0% { box-shadow: 0 0 8px 0px rgba(31, 111, 235, 0.5); } 50% { box-shadow: 0 0 16px 4px rgba(31, 111, 235, 0.8); } 100% { box-shadow: 0 0 8px 0px rgba(31, 111, 235, 0.5); } }
        .standby-pulse { animation: standby-pulse 2s infinite; }
        @keyframes urgent-pulse { 0% { box-shadow: 0 0 8px 0px rgba(218, 54, 51, 0.6); } 50% { box-shadow: 0 0 16px 4px rgba(218, 54, 51, 1); } 100% { box-shadow: 0 0 8px 0px rgba(218, 54, 51, 0.6); } }
        .urgent-pulse { animation: urgent-pulse 1.2s infinite; }
        @keyframes urgent-pulse-yellow { 0% { box-shadow: 0 0 0px 0px rgba(226, 179, 19, 0.3); } 50% { box-shadow: 0 0 12px 3px rgba(226, 179, 19, 0.7); } 100% { box-shadow: 0 0 0px 0px rgba(226, 179, 19, 0.3); } }
        .flash-warning { animation: urgent-pulse-yellow 1.5s; border-radius: 4px; }
        .flash-warning-permanent { box-shadow: 0 0 12px 3px rgba(226, 179, 19, 0.7) !important; border-radius: 4px; }
        
        /* --- [START] FINAL UI REFINEMENTS --- */
        .cockpit-wrapper { width: 100vw; height: 100vh; padding: 16px; position: relative; }
        .cockpit-container { display: flex; flex-direction: column; height: 100%; width: 100%; gap: 16px; }
        .main-view { flex: 1 1 auto; display: flex; gap: 16px; min-height: 0; }
        #bottom-bar { flex: 0 0 auto; display: flex; flex-direction: column; min-height: 0; gap: 8px; }

        #left-col { flex: 1 1 60%; display: grid; grid-template-rows: auto 1fr; gap: 16px; min-width: 300px; }
        #battle-planner-col, #analysis-col { position: relative; flex-shrink: 0; display: flex; transition: all 0.3s ease-in-out; }
        #battle-planner-col { flex-basis: 25%; min-width: 240px; }
        #analysis-col { flex-basis: 15%; min-width: 240px; }

        .panel-column.collapsed { flex-basis: 0 !important; min-width: 0 !important; padding: 0; margin: 0; overflow: hidden; }
        .card { background-color: var(--color-bg-panel); border: 1px solid var(--color-border); border-radius: 8px; padding: 16px; display: flex; flex-direction: column; backdrop-filter: blur(5px); width: 100%; height: 100%; overflow: hidden; transition: opacity 0.2s ease, visibility 0.2s; }
        .card-header { display: flex; justify-content: space-between; align-items: center; color: var(--color-text-secondary); font-size: 0.9rem; flex-shrink: 0; cursor: pointer; }
        .card-header:hover { background-color: rgba(255, 255, 255, 0.05); }
        .panel-column.collapsed .card { opacity: 0; visibility: hidden; }
        .side-panel-content { flex-grow: 1; display: flex; flex-direction: column; overflow: auto; }
        
        .v-splitter { cursor: ew-resize; width: var(--v-splitter-size); margin: 0 calc(-1 * var(--v-splitter-size) / 2); z-index: 10; background-color: transparent; flex-shrink: 0; position: relative; }
        
        .flyout-tab {
            position: absolute; right: 2px;
            background: none; border: none;
            color: var(--color-text-secondary); font-weight: 500; font-size: 0.8rem;
            letter-spacing: 1px; text-transform: uppercase; cursor: pointer;
            transition: color 0.2s ease;
            padding: 8px 4px;
            display: none;
            writing-mode: vertical-rl; text-orientation: mixed;
        }
        .flyout-tab:hover { color: var(--color-text-primary); }
        #bp-flyout-tab { top: 40%; }
        #an-flyout-tab { top: 55%; }
        body.bp-collapsed #bp-flyout-tab { display: block; }
        body.an-collapsed #an-flyout-tab { display: block; }

        #bottom-bar-header-tabs { display: flex; align-items: center; gap: 16px; padding-left: 8px;}
        .bottom-header-btn {
            background: none; border: none; color: var(--color-text-secondary);
            cursor: pointer; font-size: 0.8rem; font-weight: 500; padding: 4px 0 8px 0;
            position: relative; text-transform: uppercase; letter-spacing: 1px;
            border-bottom: 2px solid transparent;
        }
        .bottom-header-btn.active { color: var(--color-accent-primary); font-weight: 700; border-bottom-color: var(--color-accent-primary); }
        
        #situational-awareness-card { padding-top: 8px; background: none; border: none; }
        .expander-section { border-bottom: none; }
        .expander-content { max-height: 0; overflow: hidden; transition: max-height 0.3s ease-out; }
        
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 8px; font-size: 0.85rem; text-align: left; white-space: nowrap; }
        thead { border: none !important; }
        tfoot { border: none !important; }
        tfoot tr { background-color: rgba(13, 17, 23, 0.8); font-weight: 700; }
        .btn-close-pos { background-color: var(--color-danger); color: white; border: none; border-radius: 4px; padding: 2px 8px; font-size: 0.75rem; cursor: pointer; }
        /* --- [END] FINAL UI REFINEMENTS --- */
    </style>
</head>
<body>
    <div class="cockpit-wrapper">
        <div class="ignition-module" id="ignition-module">
            <h2 class="header">PRE-FLIGHT CHECK</h2>
            <input type="number" id="openingBalanceInput" placeholder="Waiting for connection..." step="0.01" readonly>
            <button id="startTradingBtn" disabled>CONNECTING...</button>
        </div>
        <main class="cockpit-container hidden" id="cockpit-container">
            <div class="main-view" id="main-view">
                <div id="left-col">
                    <div id="hud-bar"></div>
                    <div id="chart-area" class="card"><div id="tradingview-widget-container"></div></div>
                </div>
                <div class="v-splitter" id="splitter1"></div>
                <div id="battle-planner-col"></div>
                <div class="v-splitter" id="splitter2"></div>
                <div id="analysis-col"></div>
            </div>
            <div class="h-splitter" id="h-splitter"></div>
            <div id="bottom-bar"></div>
        </main>
        <div id="bp-flyout-tab" class="flyout-tab">Planner</div>
        <div id="an-flyout-tab" class="flyout-tab">Analysis</div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script type="text/javascript" src="https://s3.tradingview.com/tv.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const hudHTML = `<div class="card hud-card" style="border: none; box-shadow: none; flex-grow: 1;"><h4 class="card-header">LIVE EQUITY</h4><div id="live-equity-value" class="value">$0.00</div><div class="sub-value">Daily P/L: <span id="unrealized-pl-numbers">$0.00 (0.00%)</span></div><div class="progress-bar-container" style="margin-top: 8px; height: 4px;"><div id="pl-to-target-bar" class="progress-bar bg-blue"></div></div><div class="portfolio-info-bar"><span><strong id="portfolio-name-display">N/A</strong> (<strong id="mt5-account-display">N/A</strong>)</span><div id="hud-status-indicator" class="status-indicator-hud">● Connecting...</div></div></div><div class="card hud-card" style="border: none; box-shadow: none; flex-grow: 1;" id="daily-risk-card"><div class="card-header"><h4>DAILY RISK</h4><select class="risk-dial" id="risk-dial"><option value="0.005">0.5%</option><option value="0.01" selected>1%</option><option value="0.02">2%</option></select></div><div class="value" id="risk-left-value">$0.00</div><div class="sub-value">Risk Left</div><div class="progress-bar-container" style="margin-top: 6px;"><div id="risk-progress-bar" class="progress-bar bg-gradient-risk"></div></div><div class="progress-footer"><span id="risk-used-display">Used: $0.00</span><span id="risk-available-text">Available: $0.00</span></div></div><div class="card hud-card" style="border: none; box-shadow: none; flex-grow: 1;"><h4 class="card-header">SESSION P/L</h4><div id="session-pl-value" class="value">$0.00</div><div class="sub-value">/ Target: <span id="pl-target-value">$0.00</span></div><div class="progress-bar-container" style="margin-top: 6px;"><div id="session-pl-progress-bar" class="progress-bar bg-blue"></div></div><div class="progress-footer"><span id="opening-balance-display">Balance: $0.00</span><span id="session-pl-percent">0.00%</span></div></div>`;
        const battlePlannerHTML = `<div id="battle-planner-expander" class="card expander-panel"><div class="card-header"><h3>BATTLE PLANNER</h3></div><div class="side-panel-content"><div class="order-type-tabs"><button class="tab-btn active" data-value="LIMIT">Limit</button><button class="tab-btn" data-value="MARKET">Market</button><button class="tab-btn" data-value="STOP">Stop</button></div><div class="planner-inputs" style="display: flex; flex-direction: column; gap: 12px;"><div style="display: flex; gap: 12px; align-items: flex-end;"><div class="planner-group" style="flex: 2.5 1 0%;"><label>Symbol</label><select id="symbol-select" class="planner-select"></select></div><div class="planner-group" style="flex: 1.5 1 0%;"><label>Lot Size</label><div class="lot-input-wrapper"><input type="number" id="lot-size-input" class="planner-input" value="0.00" step="0.01"><div class="lot-adjust-controls"><button id="lot-decrease-btn" class="btn-lot-adjust">-</button><button id="lot-increase-btn" class="btn-lot-adjust">+</button></div></div></div><div class="planner-group hidden" id="entry-price-group" style="flex: 2.5 1 0%;"><label>Entry Price</label><input type="number" id="entry-price" class="planner-input" step="0.01"></div></div><div class="planner-group"><label for="tpsl-checkbox" style="cursor: pointer; padding: 4px 0;"><input type="checkbox" id="tpsl-checkbox" checked> Use TP / SL</label></div><div class="tpsl-group" style="display: flex; gap: 12px; align-items: flex-end;"><div class="planner-group" style="flex: 2.5 1 0%;"><label>Take Profit</label><input type="number" id="take-profit" class="planner-input" step="0.01"></div><div class="planner-group" style="flex: 2.5 1 0%;"><label>Stop Loss</label><input type="number" id="stop-loss" class="planner-input" step="0.01"></div><div class="planner-group" style="flex: 1 1 0%;"><button class="btn-planner" id="calculate-btn" style="width: 100%; height: 38px; font-size: 0.8rem; padding: 0 4px;">CALCULATE</button></div></div></div><div class="execution-controls" style="margin-top: 16px; display: flex; gap: 10px;"><button class="btn-planner" id="arm-btn" style="flex-grow: 1;" disabled>ARM</button><button class="btn-planner" id="execute-btn" style="flex-grow: 1;" disabled>EXECUTE</button></div><div class="planner-readouts"><div class="readout-item"><span>Potential Loss:</span><div><span id="potential-loss-value">-$0.00</span><span id="potential-loss-percent" class="loss-percent"></span></div></div><div class="readout-item"><span>Potential Profit:</span><span id="potential-profit-value" class="text-profit">+$0.00</span></div><div class="readout-item"><span>R/R Ratio:</span><span id="rr-ratio-value">-</span></div></div><select id="order-type-hidden" class="planner-select" style="display: none;"><option value="MARKET_BUY">MARKET_BUY</option><option value="MARKET_SELL">MARKET_SELL</option><option value="BUY_LIMIT" selected>BUY_LIMIT</option><option value="SELL_LIMIT">SELL_LIMIT</option><option value="BUY_STOP">BUY_STOP</option><option value="SELL_STOP">SELL_STOP</option></select></div></div>`;
        const analysisHTML = `<div id="analysis-expander" class="card expander-panel"><div class="card-header"><h3>MULTI-TIMEFRAME ANALYSIS</h3></div><div class="side-panel-content"><p>Analysis content goes here...</p></div></div>`;
        const bottomBarHTML = `<div id="bottom-bar-header-tabs"><button class="bottom-header-btn" data-target="open-positions-section">Open Positions</button><button class="bottom-header-btn" data-target="pending-orders-section">Pending Orders</button><button class="bottom-header-btn" data-target="trade-history-section">Trade History</button></div><div id="situational-awareness-card"><div id="open-positions-section" class="expander-section"><div class="expander-content"><div class="table-container"><table><thead><tr><th>Time</th><th>Symbol</th><th>Type</th><th>Volume</th><th>Price</th><th>SL</th><th>TP</th><th>Risk $</th><th>Profit</th><th>Action</th></tr></thead><tbody id="open-positions-tbody"></tbody><tfoot><tr><td colspan="7" style="text-align: right;"><strong>TOTAL</strong></td><td id="total-open-risk" class="text-loss">$0.00</td><td id="total-open-profit">$0.00</td><td></td></tr></tfoot></table></div></div></div><div id="pending-orders-section" class="expander-section"><div class="expander-content"><div class="table-container"><table><thead><tr><th>Ticket</th><th>Time</th><th>Symbol</th><th>Type</th><th>Volume</th><th>Entry Price</th><th>SL</th><th>TP</th></tr></thead><tbody id="pending-orders-tbody"></tbody></table></div></div></div><div id="trade-history-section" class="expander-section"><div class="expander-content"><div class="table-container"><table><thead><tr><th>Close Time</th><th>Symbol</th><th>Type</th><th>Volume</th><th>Profit</th></tr></thead><tbody id="trade-history-tbody"></tbody><tfoot><tr><td colspan="4" style="text-align: right;"><strong>TOTAL</strong></td><td id="total-history-profit">$0.00</td></tr></tfoot></table></div></div></div></div>`;

        document.getElementById('hud-bar').innerHTML = hudHTML; 
        document.getElementById('battle-planner-col').innerHTML = battlePlannerHTML;
        document.getElementById('analysis-col').innerHTML = analysisHTML;
        document.getElementById('bottom-bar').innerHTML = bottomBarHTML;
        
        const selectors = { body: document.body, ignitionModule: document.getElementById('ignition-module'), cockpitContainer: document.getElementById('cockpit-container'), startTradingBtn: document.getElementById('startTradingBtn'), openingBalanceInput: document.getElementById('openingBalanceInput'), liveEquityValue: document.getElementById('live-equity-value'), unrealizedPlNumbers: document.getElementById('unrealized-pl-numbers'), riskLeftValue: document.getElementById('risk-left-value'), riskUsedDisplay: document.getElementById('risk-used-display'), riskAvailableText: document.getElementById('risk-available-text'), riskProgressBar: document.getElementById('risk-progress-bar'), sessionPlValue: document.getElementById('session-pl-value'), plTargetValue: document.getElementById('pl-target-value'), openingBalanceDisplay: document.getElementById('opening-balance-display'), sessionPlPercent: document.getElementById('session-pl-percent'), sessionPlProgressBar: document.getElementById('session-pl-progress-bar'), riskDial: document.getElementById('risk-dial'), openPositionsTableBody: document.getElementById('open-positions-tbody'), pendingOrdersTableBody: document.getElementById('pending-orders-tbody'), tradeHistoryTableBody: document.getElementById('trade-history-tbody'), symbolSelect: document.getElementById('symbol-select'), orderTypeSelect: document.getElementById('order-type-hidden'), entryPriceGroup: document.getElementById('entry-price-group'), entryPriceInput: document.getElementById('entry-price'), stopLossInput: document.getElementById('stop-loss'), takeProfitInput: document.getElementById('take-profit'), calculateBtn: document.getElementById('calculate-btn'), armBtn: document.getElementById('arm-btn'), executeBtn: document.getElementById('execute-btn'), potentialLossValue: document.getElementById('potential-loss-value'), potentialLossPercent: document.getElementById('potential-loss-percent'), potentialProfitValue: document.getElementById('potential-profit-value'), rrRatioValue: document.getElementById('rr-ratio-value'), lotSizeInput: document.getElementById('lot-size-input'), tpslCheckbox: document.getElementById('tpsl-checkbox'), hudStatusIndicator: document.getElementById('hud-status-indicator'), portfolioNameDisplay: document.getElementById('portfolio-name-display'), mt5AccountDisplay: document.getElementById('mt5-account-display'), totalOpenRisk: document.getElementById('total-open-risk'), totalOpenProfit: document.getElementById('total-open-profit'), totalHistoryProfit: document.getElementById('total-history-profit'), battlePlannerCard: document.getElementById('battle-planner-expander'), plToTargetBar: document.getElementById('pl-to-target-bar'), mainView: document.getElementById('main-view'), leftCol: document.getElementById('left-col'), battlePlannerCol: document.getElementById('battle-planner-col'), analysisCol: document.getElementById('analysis-col'), splitter1: document.getElementById('splitter1'), splitter2: document.getElementById('splitter2'), hSplitter: document.getElementById('h-splitter'), bottomBar: document.getElementById('bottom-bar'), analysisExpander: document.getElementById('analysis-expander'), lotDecreaseBtn: document.getElementById('lot-decrease-btn'), lotIncreaseBtn: document.getElementById('lot-increase-btn'), dailyRiskCard: document.getElementById('daily-risk-card'), 
        bpFlyoutTab: document.getElementById('bp-flyout-tab'), anFlyoutTab: document.getElementById('an-flyout-tab'), situationalAwarenessCard: document.getElementById('situational-awareness-card')
        };
        
        let AppState = {}; let tradingViewWidget = null; let lastCalculation = {}; let animationTimeout = null;
        const socket = new WebSocket('ws://localhost:5555');

        socket.onopen = () => { selectors.hudStatusIndicator.textContent = '● Connected'; selectors.hudStatusIndicator.style.color = 'var(--color-success)'; };
        socket.onclose = () => { selectors.hudStatusIndicator.textContent = '● Disconnected'; selectors.hudStatusIndicator.style.color = 'var(--color-danger)'; selectors.ignitionModule.style.display = 'flex'; selectors.cockpitContainer.classList.add('hidden'); };
        socket.onerror = () => { selectors.hudStatusIndicator.textContent = '● Error'; selectors.hudStatusIndicator.style.color = 'var(--color-danger)'; };
        socket.onmessage = (event) => { const serverData = JSON.parse(event.data); if (serverData.status === "READY_TO_START") { selectors.openingBalanceInput.value = serverData.suggested_balance.toFixed(2); selectors.startTradingBtn.disabled = false; selectors.startTradingBtn.textContent = "START TRADING DAY"; } else if (serverData.status === "SESSION_ACTIVE") { if (selectors.ignitionModule.style.display !== 'none') { selectors.ignitionModule.style.display = 'none'; selectors.cockpitContainer.classList.remove('hidden'); } updateFullDashboard(serverData); } if (serverData.event === "CALCULATE_LOT_RESPONSE") { lastCalculation = serverData.payload; updatePlannerReadouts(lastCalculation.lot_size); if (lastCalculation.lot_size > 0) { selectors.armBtn.disabled = false; clearTimeout(animationTimeout); selectors.dailyRiskCard.classList.remove('flash-warning-permanent'); selectors.stopLossInput.parentElement.classList.remove('flash-warning-permanent'); selectors.dailyRiskCard.classList.add('flash-warning'); animationTimeout = setTimeout(() => { selectors.dailyRiskCard.classList.remove('flash-warning'); selectors.armBtn.classList.add('standby-pulse'); }, 1500); } else { selectors.armBtn.disabled = true; const riskLeft = AppState.risk_left || 0; alert( `RISK LIMIT EXCEEDED\n\n` + `Your available risk budget is only: $${riskLeft.toFixed(2)}\n\n` + `The risk for your current trade plan is higher than this amount.\n` + `Please adjust your Stop Loss to be closer to the entry price.` ); selectors.stopLossInput.parentElement.classList.add('flash-warning-permanent'); } } };
        function updateFullDashboard(data) { AppState = { ...AppState, ...data }; if (selectors.symbolSelect.options.length === 0 && AppState.all_symbols && AppState.all_symbols.length > 0) { AppState.all_symbols.forEach(s => selectors.symbolSelect.add(new Option(s,s))); $('#symbol-select').select2({ theme: "obsidian" }); const defaultSymbol = [...selectors.symbolSelect.options].some(o => o.value.includes('XAUUSD')) ? [...selectors.symbolSelect.options].find(o => o.value.includes('XAUUSD')).value : AppState.all_symbols[0]; $('#symbol-select').val(defaultSymbol).trigger('change'); } const profitColor = 'var(--color-success)', lossColor = 'var(--color-danger)'; selectors.portfolioNameDisplay.textContent = data.portfolio_context?.PortfolioName || 'N/A'; selectors.mt5AccountDisplay.textContent = data.mt5_account_id || 'N/A'; selectors.liveEquityValue.textContent = `$${(data.equity || 0).toLocaleString(undefined, {minimumFractionDigits: 2, maximumFractionDigits: 2})}`; selectors.liveEquityValue.style.color = data.todays_total_pl > 0 ? profitColor : data.todays_total_pl < 0 ? lossColor : 'inherit'; selectors.unrealizedPlNumbers.textContent = `$${(data.todays_total_pl || 0).toFixed(2)} (${(data.todays_total_pl_percent || 0).toFixed(2)}%)`; selectors.unrealizedPlNumbers.style.color = data.todays_total_pl > 0 ? profitColor : data.todays_total_pl < 0 ? lossColor : 'inherit'; const plToTargetPercent = data.pl_target > 0 ? ((data.unrealized_pl + data.realized_pl) / data.pl_target) * 100 : 0; selectors.plToTargetBar.style.width = `${Math.min(Math.max(plToTargetPercent, 0), 100)}%`; selectors.riskAvailableText.textContent = `Available: $${(data.available_risk || 0).toFixed(2)}`; const riskUsedPercent = data.available_risk > 0 ? (data.risk_used / data.available_risk) * 100 : 0; selectors.riskUsedDisplay.textContent = `Used: $${(data.risk_used || 0).toFixed(2)} (${riskUsedPercent.toFixed(2)}%)`; selectors.riskProgressBar.style.width = `${Math.min(riskUsedPercent, 100)}%`; if (data.risk_left <= 0) { selectors.riskLeftValue.textContent = "RISK LIMIT REACHED"; selectors.riskLeftValue.style.color = lossColor; selectors.calculateBtn.disabled = true; selectors.armBtn.disabled = true; selectors.executeBtn.disabled = true; } else { selectors.riskLeftValue.textContent = `$${(data.risk_left || 0).toFixed(2)}`; const riskLeftPercent = (data.risk_left / data.available_risk); selectors.riskLeftValue.style.color = `rgb(${Math.round(218+(35-218)*(1-riskLeftPercent))}, ${Math.round(54+(134-54)*riskLeftPercent)}, 51)`; selectors.calculateBtn.disabled = false; } selectors.openingBalanceDisplay.textContent = `Balance: $${(data.opening_balance || 0).toLocaleString(undefined, {minimumFractionDigits: 2, maximumFractionDigits: 2})}`; selectors.sessionPlValue.textContent = `$${(data.realized_pl || 0).toLocaleString(undefined, {minimumFractionDigits: 2, maximumFractionDigits: 2})}`; selectors.sessionPlValue.style.color = data.realized_pl > 0 ? profitColor : data.realized_pl < 0 ? lossColor : 'inherit'; selectors.plTargetValue.textContent = `$${(data.pl_target || 0).toFixed(2)}`; const plPercentOfTarget = data.pl_target > 0 ? (data.realized_pl / data.pl_target) * 100 : 0; selectors.sessionPlProgressBar.style.width = `${Math.min(Math.max(plPercentOfTarget, 0), 100)}%`; selectors.sessionPlPercent.textContent = `${plPercentOfTarget.toFixed(2)}%`; renderTable(selectors.openPositionsTableBody, data.open_positions || [], createOpenPositionRow, 10, "No open positions"); renderTable(selectors.pendingOrdersTableBody, data.pending_orders || [], createPendingOrderRow, 8, "No pending orders"); renderTable(selectors.tradeHistoryTableBody, data.trade_history || [], createTradeHistoryRow, 5, "No trade history for today"); const totalOpenRisk = data.open_positions_total_risk || 0; selectors.totalOpenRisk.textContent = `$${totalOpenRisk.toFixed(2)}`; const totalOpenProfit = data.open_positions_total_profit || 0; selectors.totalOpenProfit.textContent = `$${totalOpenProfit.toFixed(2)}`; selectors.totalOpenProfit.style.color = totalOpenProfit >= 0 ? profitColor : lossColor; const totalHistoryProfit = data.history_total_profit || 0; selectors.totalHistoryProfit.textContent = `$${totalHistoryProfit.toFixed(2)}`; selectors.totalHistoryProfit.style.color = totalHistoryProfit >= 0 ? profitColor : lossColor; }
        function renderTable(tbody, data, rowCreator, colspan, emptyMessage) { tbody.innerHTML = ''; if (data.length === 0) { tbody.innerHTML = `<tr><td colspan="${colspan}" style="text-align:center; color: var(--color-text-secondary);">${emptyMessage}</td></tr>`; return; } data.forEach(item => tbody.insertAdjacentHTML('beforeend', rowCreator(item))); }
        function createOpenPositionRow(pos) { const profitColor = pos.Profit >= 0 ? 'text-profit' : 'text-loss'; return `<tr><td>${pos.Time || ''}</td><td>${pos.Symbol}</td><td>${pos.Type}</td><td>${pos.Volume}</td><td>${pos.Price}</td><td>${pos.SL || ''}</td><td>${pos.TP || ''}</td><td class="text-loss">$${(pos.position_risk || 0).toFixed(2)}</td><td class="${profitColor}">$${(pos.Profit || 0).toFixed(2)}</td><td><button class="btn-close-pos" data-ticket="${pos.Ticket}">Close</button></td></tr>`; }
        function createPendingOrderRow(ord) { return `<tr><td>${ord.Ticket}</td><td>${ord.Time || ''}</td><td>${ord.Symbol}</td><td>${ord.Type}</td><td>${ord.Lots}</td><td>${ord['Entry Price']}</td><td>${ord.SL}</td><td>${ord.TP}</td></tr>`; }
        function createTradeHistoryRow(trade) { const profitColor = trade.Profit >= 0 ? 'text-profit' : 'text-loss'; return `<tr><td>${trade['Close Time'] || ''}</td><td>${trade.Symbol || ''}</td><td>${trade.Type || ''}</td><td>${trade.Volume || 0}</td><td class="${profitColor}">$${(trade.Profit || 0).toFixed(2)}</td></tr>`; }
        function resetCalculator(fullReset = true) { if (fullReset) { selectors.lotSizeInput.value = '0.00'; if (selectors.tpslCheckbox.checked) { selectors.stopLossInput.value = ''; selectors.takeProfitInput.value = ''; } } selectors.potentialLossValue.textContent = '-$0.00'; selectors.potentialLossPercent.textContent = ''; selectors.potentialProfitValue.textContent = '+$0.00'; selectors.rrRatioValue.textContent = '-'; selectors.armBtn.disabled = true; selectors.executeBtn.disabled = true; selectors.armBtn.classList.remove('standby-pulse'); selectors.executeBtn.classList.remove('urgent-pulse'); selectors.dailyRiskCard.classList.remove('flash-warning'); selectors.situationalAwarenessCard.classList.remove('order-sent-flash'); clearTimeout(animationTimeout); lastCalculation = {}; }
        function renderTradingViewWidget(symbol) { if (!symbol) return; const cleanSymbol = symbol.replace(/_$/, ''); const tvSymbol = `OANDA:${cleanSymbol}`; if (tradingViewWidget && tradingViewWidget.options && tradingViewWidget.options.symbol !== tvSymbol) { tradingViewWidget.setSymbol(tvSymbol, "15", () => {}); } else if (!tradingViewWidget) { tradingViewWidget = new TradingView.widget({ "container_id": "tradingview-widget-container", "autosize": true, "symbol": tvSymbol, "interval": "15", "timezone": "Etc/UTC", "theme": "dark", "style": "1", "locale": "en", "enable_publishing": false, "hide_side_toolbar": false, "details": true, "studies": ["MASimple@tv-basicstudies"] }); } }
        function updatePlannerReadouts(currentLot) { currentLot = parseFloat(currentLot) || 0; if (!lastCalculation.lot_size || lastCalculation.lot_size === 0 || currentLot <= 0) { selectors.lotSizeInput.value = currentLot > 0 ? currentLot.toFixed(2) : '0.00'; resetCalculator(false); selectors.potentialLossValue.classList.remove('text-profit', 'text-warning', 'text-loss'); selectors.potentialLossPercent.classList.remove('text-profit', 'text-warning', 'text-loss'); return; } const lotRatio = currentLot / lastCalculation.lot_size; let potentialLoss = (lastCalculation.potential_loss_usd || 0) * lotRatio; if (AppState.risk_left > 0 && potentialLoss > AppState.risk_left) { const maxLotRatio = AppState.risk_left / (lastCalculation.potential_loss_usd || 1); currentLot = lastCalculation.lot_size * maxLotRatio; potentialLoss = AppState.risk_left; } selectors.lotSizeInput.value = currentLot.toFixed(2); const potentialProfit = (lastCalculation.potential_profit_usd || 0) * (currentLot / lastCalculation.lot_size); selectors.potentialLossValue.textContent = `-$${potentialLoss.toFixed(2)}`; selectors.potentialProfitValue.textContent = `+$${potentialProfit.toFixed(2)}`; selectors.rrRatioValue.textContent = (potentialLoss > 0) ? (potentialProfit / potentialLoss).toFixed(2) : '-'; const lossPercentOfRiskLeft = (AppState.risk_left > 0 && potentialLoss > 0) ? (potentialLoss / AppState.risk_left) * 100 : 0; selectors.potentialLossPercent.textContent = `(${(lossPercentOfRiskLeft).toFixed(2)}% of Risk Left)`; selectors.potentialLossValue.classList.remove('text-profit', 'text-warning', 'text-loss'); selectors.potentialLossPercent.classList.remove('text-profit', 'text-warning', 'text-loss'); if (lossPercentOfRiskLeft > 50) { selectors.potentialLossValue.classList.add('text-loss'); selectors.potentialLossPercent.classList.add('text-loss'); } else if (lossPercentOfRiskLeft > 25) { selectors.potentialLossValue.classList.add('text-warning'); selectors.potentialLossPercent.classList.add('text-warning'); } else if (lossPercentOfRiskLeft > 0) { selectors.potentialLossValue.classList.add('text-profit'); selectors.potentialLossPercent.classList.add('text-profit'); } }
        
        // --- [START] FINAL LAYOUT MANAGER ---
        const layoutManager = {
            init() {
                selectors.battlePlannerCol.classList.add('panel-column');
                selectors.analysisCol.classList.add('panel-column');
                
                this.initSplitter(selectors.splitter1, selectors.leftCol, selectors.battlePlannerCol);
                this.initSplitter(selectors.splitter2, selectors.battlePlannerCol, selectors.analysisCol);
                this.initSplitter(selectors.hSplitter, selectors.mainView, selectors.bottomBar);

                selectors.battlePlannerCard.querySelector('.card-header').addEventListener('click', () => this.togglePanel(selectors.battlePlannerCol, 'bp-collapsed'));
                selectors.analysisExpander.querySelector('.card-header').addEventListener('click', () => this.togglePanel(selectors.analysisCol, 'an-collapsed'));
                selectors.bpFlyoutTab.addEventListener('click', () => this.togglePanel(selectors.battlePlannerCol, 'bp-collapsed'));
                selectors.anFlyoutTab.addEventListener('click', () => this.togglePanel(selectors.analysisCol, 'an-collapsed'));
                
                document.querySelectorAll('.bottom-header-btn').forEach(tab => {
                    tab.addEventListener('click', () => {
                        const section = document.getElementById(tab.dataset.target);
                        const content = section.querySelector('.expander-content');
                        const wasActive = tab.classList.contains('active');

                        tab.classList.toggle('active', !wasActive);
                        content.style.maxHeight = !wasActive ? content.scrollHeight + "px" : null;
                    });
                });
                document.querySelector('.bottom-header-btn[data-target="open-positions-section"]').click();
            },
            
            togglePanel(panelElement, bodyClass) {
                panelElement.classList.toggle('collapsed');
                selectors.body.classList.toggle(bodyClass);
            },

            initSplitter(splitterEl, elA, elB) { 
                const handleMouseMove = (e) => { 
                    const isVertical = splitterEl.classList.contains('v-splitter');
                    const rectA = elA.getBoundingClientRect();
                    if (isVertical) {
                        const totalFlexWidth = elA.offsetWidth + elB.offsetWidth;
                        const newBasisA = e.clientX - rectA.left;
                        if (newBasisA > 50 && (totalFlexWidth - newBasisA) > 50) {
                           elA.style.flexBasis = `${newBasisA}px`;
                           elB.style.flexBasis = `${totalFlexWidth - newBasisA}px`;
                        }
                    } else {
                        const totalFlexHeight = elA.offsetHeight + elB.offsetHeight;
                        const newBasisA = e.clientY - rectA.top;
                         if (newBasisA > 100 && (totalFlexHeight - newBasisA) > 100) {
                            elA.style.flexBasis = `${newBasisA}px`;
                            elB.style.flexBasis = `${totalFlexHeight - newBasisA}px`;
                         }
                    } 
                }; 
                const handleMouseUp = () => { document.removeEventListener('mousemove', handleMouseMove); document.removeEventListener('mouseup', handleMouseUp); }; 
                splitterEl.addEventListener('mousedown', (e) => { e.preventDefault(); document.addEventListener('mousemove', handleMouseMove); document.addEventListener('mouseup', handleMouseUp); }); 
            },
        };
        // --- [END] FINAL LAYOUT MANAGER ---
        
        selectors.startTradingBtn.addEventListener('click', () => { const balanceValue = selectors.openingBalanceInput.value; if (balanceValue && !isNaN(parseFloat(balanceValue)) && parseFloat(balanceValue) > 0) { socket.send(JSON.stringify({ event: "START_SESSION", payload: { openingBalance: parseFloat(balanceValue) } })); } else { alert("Please enter a valid opening balance."); } });
        selectors.bottomBar.addEventListener('click', (event) => { if (event.target.classList.contains('btn-close-pos')) { const ticket = event.target.dataset.ticket; if (ticket && confirm(`Are you sure you want to close position with ticket #${ticket}?`)) { socket.send(JSON.stringify({ event: "CLOSE_POSITION", payload: { ticket: parseInt(ticket) } })); } } });
        $('#symbol-select').on('change', function() { renderTradingViewWidget(this.value); resetCalculator(); });
        selectors.calculateBtn.addEventListener('click', () => { resetCalculator(false); if (!selectors.tpslCheckbox.checked || !parseFloat(selectors.stopLossInput.value)) { alert("Please check the TP/SL box and enter a valid Stop Loss price to calculate."); return; } const command = { event: "CALCULATE_LOT_REQUEST", payload: { symbol: selectors.symbolSelect.value, sl_price: parseFloat(selectors.stopLossInput.value) || 0, tp_price: parseFloat(selectors.takeProfitInput.value) || 0, risk_percent: parseFloat(selectors.riskDial.value), order_type_str: selectors.orderTypeSelect.value, entry_price: parseFloat(selectors.entryPriceInput.value) || 0 } }; if(!command.payload.symbol) { alert("Please select a Symbol."); return; } socket.send(JSON.stringify(command)); });
        selectors.armBtn.addEventListener('click', () => { selectors.armBtn.classList.remove('standby-pulse'); selectors.armBtn.disabled = true; selectors.executeBtn.disabled = false; selectors.executeBtn.classList.add('urgent-pulse'); });
        selectors.executeBtn.addEventListener('click', () => { const orderDetails = { type: selectors.orderTypeSelect.value, lot_size: parseFloat(selectors.lotSizeInput.value), symbol: selectors.symbolSelect.value, sl: parseFloat(selectors.stopLossInput.value) || 0, tp: parseFloat(selectors.takeProfitInput.value) || 0, entry_price: parseFloat(selectors.entryPriceInput.value) || 0 }; if (orderDetails.lot_size <= 0) { alert("Invalid Lot Size."); return; } socket.send(JSON.stringify({ event: "EXECUTE_TRADE", payload: orderDetails })); selectors.executeBtn.classList.remove('urgent-pulse'); selectors.situationalAwarenessCard.classList.add('order-sent-flash'); setTimeout(() => { selectors.situationalAwarenessCard.classList.remove('order-sent-flash'); }, 1500); resetCalculator(); });
        const tabButtons = document.querySelectorAll('.tab-btn'); tabButtons.forEach(button => { button.addEventListener('click', () => { tabButtons.forEach(btn => btn.classList.remove('active')); button.classList.add('active'); const category = button.dataset.value; const isMarket = category === 'MARKET'; selectors.entryPriceGroup.classList.toggle('hidden', isMarket); selectors.orderTypeSelect.value = isMarket ? 'MARKET_BUY' : (category === 'LIMIT' ? 'BUY_LIMIT' : 'BUY_STOP'); selectors.orderTypeSelect.dispatchEvent(new Event('change')); }); });
        selectors.orderTypeSelect.addEventListener('change', () => { resetCalculator(); });
        const tpslGroups = document.querySelectorAll('.tpsl-group'); selectors.tpslCheckbox.addEventListener('change', () => { tpslGroups.forEach(g => g.style.display = selectors.tpslCheckbox.checked ? 'flex' : 'none'); resetCalculator(); });
        let lotAdjInterval; function startAdjustingLot(isIncreasing) { let currentLot = parseFloat(selectors.lotSizeInput.value) || 0; const lotStep = 0.01; currentLot += isIncreasing ? lotStep : -lotStep; if (currentLot < 0) currentLot = 0; updatePlannerReadouts(currentLot); } function stopAdjustingLot() { clearInterval(lotAdjInterval); }
        selectors.lotDecreaseBtn.addEventListener('mousedown', () => { startAdjustingLot(false); lotAdjInterval = setInterval(() => startAdjustingLot(false), 100); });
        selectors.lotIncreaseBtn.addEventListener('mousedown', () => { startAdjustingLot(true); lotAdjInterval = setInterval(() => startAdjustingLot(true), 100); });
        document.addEventListener('mouseup', stopAdjustingLot); document.addEventListener('mouseleave', stopAdjustingLot);
        selectors.lotSizeInput.addEventListener('input', (e) => updatePlannerReadouts(e.target.value));
        
        layoutManager.init();
        
        document.querySelector('.tab-btn.active').click();
        selectors.tpslCheckbox.dispatchEvent(new Event('change'));
    });
</script>
</body>
</html>